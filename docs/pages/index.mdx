import Image from 'next/image'
import prefix from '../lib/config';

<div className="flex flex-col items-center justify-center text-center">
    <h1 className="text-6xl font-bold mt-8">NaturalAgents</h1>
    <p className="text-lg">Anyone can build agents!</p>
    <div className="w-full max-w-4xl mt-8 aspect-w-16 aspect-h-9 border-4 border-current rounded-lg dark:border-0">
        <video
        className="w-full h-full rounded-lg"
        controls
        autoPlay
        muted
        loop
        src={`${prefix}/static/videos/NaturalAgents Demo.mp4`}
        type="video/mp4"
        >
        </video>
    </div>

  <h2 className="text-3xl font-semibold mt-16 mb-8">
    Build personalized and custom agents in seconds
  </h2>
  <div className="grid grid-cols-1 md:grid-cols-3 gap-12 mt-12 w-full max-w-4xl">
    <div className="flex flex-col items-center p-4 border rounded-lg shadow-lg">
      <h3 className="text-xl font-semibold">No code</h3>
      <p className="mt-1">Use plain English to build agents for your use case.</p>
    </div>
    <div className="flex flex-col items-center p-4 border rounded-lg shadow-lg">
      <h3 className="text-xl font-semibold">Easy to use</h3>
      <p className="mt-1">Simple, pre-built macros that build agents in a notion-style editor.</p>
    </div>
    <div className="flex flex-col items-center p-4 border rounded-lg shadow-lg">
      <h3 className="text-xl font-semibold">Collaborative</h3>
      <p className="mt-1">Save agent recipes and resuse them as desired.</p>
    </div>
  </div>

  <div className="flex flex-col items-center text-center mt-16 space-y-4">
    <h2 className="text-3xl font-semibold">Sounds exciting? We're looking for collaborators</h2>
    <a
      href="https://github.com/NaturalAgents/NaturalAgents"
      target="_blank"
      rel="noopener noreferrer"
      className="inline-flex flex-row items-center px-6 py-3 text-white font-semibold rounded-lg transition hover:opacity-90"
      style={{background: "linear-gradient(135deg, #ff4f00, #ee0024, #850056, #00519c)"}}
    >
      <Image
        src={`${prefix}/static/images/github-mark-white.svg`}
        alt="GitHub"
        width={10}
        height={10}
        className="w-5 h-5 mr-2"
      />
      <span>Join us on GitHub!</span>
    </a>
  </div>


</div>
